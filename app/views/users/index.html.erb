<h1>Users</h1>

<% @users.each do |user| %>
  <%= attachment_image_tag user, :profile_image, format: 'jpg', size:"40x40", fallback: "no_image.jpg" %>

  <%= user.name %>

  <%= link_to 'Show', user_path(user.id) %>
<% end %>